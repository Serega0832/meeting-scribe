# Meeting Scribe 🎙️

Веб-приложение для автоматической транскрибации и саммаризации встреч. 
Загружайте аудио или видео (даже длиной в 2-3 часа), и система превратит их в структурированный текст с итогами, задачами и договоренностями.

Построено на стеке: **FastAPI + HTMX + Tailwind + SQLite**.
Использует **Gemini API** (через прокси) для распознавания и анализа.

![Screenshot](https://via.placeholder.com/800x400?text=Interface+Screenshot+Place+Here)

## ✨ Особенности

-   **Drag & Drop интерфейс:** Удобная загрузка файлов.
-   **Поддержка любых форматов:** MP3, WAV, MP4, MOV, MKV (автоматическая конвертация и сжатие).
-   **Реактивная скорость (Turbo Mode):**
    -   Сжимает аудио в легкий формат (32kbps mono) для мгновенной отправки.
    -   Нарезает длинные записи на куски по 10 минут.
    -   Отправляет их **параллельно** (AsyncIO), сокращая время обработки 2-часового видео с 20 минут до 1-2 минут.
-   **Умное Саммари:** Генерирует красивые Markdown-отчеты с итогами встречи, сроками и ответственными.
-   **SPA-ощущение:** Благодаря HTMX страница не перезагружается полностью, добавление записей происходит динамически.

## 🛠️ Технологии

*   **Backend:** Python 3.10+, FastAPI, SQLModel (SQLAlchemy).
*   **Frontend:** Jinja2 Templates, Tailwind CSS (CDN), HTMX.
*   **Processing:** FFmpeg (для конвертации, сжатия и нарезки медиа).
*   **AI:** Интеграция с моделями Gemini 2.0 Flash Lite (Audio) и 1.5 Flash (Text).

## 🚀 Установка и запуск

### 1. Предварительные требования
В системе должна быть установлена утилита **ffmpeg**.
- **macOS:** `brew install ffmpeg`
- **Ubuntu:** `sudo apt install ffmpeg`
- **Windows:** Скачать ffmpeg.exe и добавить путь к нему в переменные среды (PATH).

### 2. Клонирование репозитория

```bash
git clone https://github.com/your-username/meeting-scribe.git
cd meeting-scribe
```

### 3. Виртуальное окружение

```bash
# Создание
python -m venv venv

# Активация (Windows)
venv\Scripts\activate

# Активация (macOS/Linux)
source venv/bin/activate
```

### 4. Зависимости

```bash
pip install -r requirements.txt
```

### 5. Настройка (.env)
Создайте в корне проекта файл `.env` (без названия, просто точка и env). Вставьте туда свои настройки. Это скроет ваш приватный API URL от посторонних глаз.

```ini
# URL вашего прокси-сервиса Gemini
EXTERNAL_API_URL=https://your-private-gemini-wrapper.ru

# Имя сервиса для логов
SERVICE_NAME=my_meeting_scribe

# Сколько кусков аудио отправлять одновременно (10-15 оптимально)
MAX_CONCURRENT_REQUESTS=10
```

### 6. Запуск

```bash
uvicorn main:app --reload
```
Откройте браузер по адресу: `http://127.0.0.1:8000`

## 🛡️ Структура проекта

```text
meeting-scribe/
├── main.py            # Единая точка входа и вся логика приложения
├── templates/         # HTML шаблоны (Jinja2)
├── static/            # JS скрипты и стили
├── uploads/           # Временное хранилище (в .gitignore)
└── database.db        # Локальная база данных (в .gitignore)
```

## 🤝 Вклад в проект
Буду рад пул-реквестам! Особенно интересны улучшения промптов для саммаризации и поддержка новых форматов файлов.